# Root .env — used by Vite frontend ONLY.
# The Gemini API key does NOT go here anymore.
# See server/.env.example for the key that stays on the server.

# (No frontend environment variables needed at this time)
